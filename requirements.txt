# وقت التشغيل — الأدوات الأساسية (بلا اعتماديات ثقيلة؛ تعمل دون اتصال بالإنترنت)
# (وحدات screening وnlp وzakat وpipelines تستخدم فقط المكتبة القياسية.)

# طبقة الواجهة البرمجية الاختيارية
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.6,<3.0

# الاختبار
pytest>=8.0,<10.0
pytest-cov>=5.0,<6.0
httpx>=0.27,<1.0

# جودة الكود
ruff>=0.4,<1.0
mypy>=1.9,<2.0

# البيانات (اختياري، للأمثلة/دفاتر الملاحظات مع مجموعات بيانات كبيرة)
pandas>=2.2,<3.0
pyyaml>=6.0,<7.0
