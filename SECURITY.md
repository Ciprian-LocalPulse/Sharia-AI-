# سياسة الأمان — Sharia-AI

## نموذج الأمان الحالي (منذ الإصدار 0.2.0)

| الطبقة | الآلية | ملاحظات |
|---|---|---|
| المصادقة | مفتاح API ثابت عبر رأس `X-API-Key` | مقارنة بزمن ثابت (`hmac.compare_digest`). إذا لم تُهيَّأ أي مفاتيح (`SHARIA_AI_API_KEYS` فارغ)، تُعطَّل المصادقة تلقائيًا — **هذا وضع تطوير محلي فقط، ويجب ضبط المفاتيح دائمًا قبل النشر في الإنتاج.** |
| تحديد معدّل الطلبات | نافذة زمنية ثابتة، في الذاكرة، لكل عميل (مفتاح API أو IP) | مناسب لعملية واحدة (single-process). عند النشر بعدة نسخ (replicas)، يجب استبداله بمخزن مشترك (Redis) — راجع خارطة الطريق. |
| CORS | مُعطَّل افتراضيًا؛ يُفعَّل فقط بضبط `SHARIA_AI_CORS_ORIGINS` صراحةً | لا يوجد `allow_origins=["*"]` في أي مكان من الكود. |
| حدود حجم المُدخلات | حد أقصى لعدد أحرف نصوص العقود (`SHARIA_AI_MAX_CONTRACT_CHARS`، افتراضيًا 50,000) | يُطبَّق عبر Pydantic `field_validator` على مستوى الطلب الواحد وعلى كل عقد داخل تقرير الامتثال المُجمَّع. |
| التسجيل (Logging) | JSON منظَّم إلى stdout، مع `request_id` لكل طلب | لا تُسجَّل قيم مفاتيح API الكاملة أو أجسام الطلبات الحسّاسة في سجلّات التطبيق. |
| سجلّ التدقيق (Audit trail) | SQLite دائم (append-only منطقيًا)، يُفعَّل افتراضيًا | يُخزِّن كل قرار امتثال صادر، مع الطابع الزمني ومعرِّف العميل. نقطة النهاية `/v1/audit/recent` تتطلّب مصادقة دائمًا. |
| معالجة الأخطاء | معالِج استثناءات عام يمنع تسرّب `stack trace` إلى المستخدم النهائي | التفاصيل الكاملة تُسجَّل داخليًا فقط. |

## ما هو خارج نطاق هذا الإصدار (معروف، وموثَّق عمدًا)

- **لا يوجد OAuth2/JWT أو صلاحيات متدرّجة (RBAC)** — مفتاح API واحد يمنح وصولاً كاملاً لكل نقاط النهاية المحمية. مناسب لتكامل خدمة-إلى-خدمة (service-to-service)، وليس لتطبيق متعدد المستخدمين بأدوار مختلفة.
- **تحديد معدّل الطلبات في الذاكرة فقط** — لا يُشارَك بين نسخ متعددة من الحاوية. عند التوسّع الأفقي (horizontal scaling)، استبدل `FixedWindowRateLimiter` بتنفيذ مبني على Redis.
- **سجلّ التدقيق SQLite محلي** — مناسب لعبء عمل خفيف/متوسط أو نشرة بنسخة واحدة. للاستخدام المؤسسي بحجم كبير، يُنصَح بالهجرة إلى قاعدة بيانات علائقية مُدارة (PostgreSQL) مع نسخ احتياطي منتظم.
- **لا يوجد تدوير تلقائي لمفاتيح API (key rotation)** — يجب إدارته يدويًا عبر متغيرات البيئة وإعادة النشر.

## الإبلاغ عن ثغرة أمنية

إذا اكتشفت ثغرة أمنية، يُرجى عدم فتح Issue علني. راسل المؤلف مباشرةً (راجع معلومات التواصل في ملف تعريف GitHub) مع وصف تفصيلي لخطوات إعادة الإنتاج.
