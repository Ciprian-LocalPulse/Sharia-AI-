# Runtime — core toolkit (fără dependențe grele; rulează offline)
# (Modulele screening, nlp, zakat, pipelines folosesc doar stdlib.)

# Strat API opțional
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.6,<3.0

# Testare
pytest>=8.0,<9.0
pytest-cov>=5.0,<6.0

# Calitate cod
ruff>=0.4,<1.0
mypy>=1.9,<2.0

# Date (opțional, pentru exemple/notebook-uri cu seturi mari de date)
pandas>=2.2,<3.0
pyyaml>=6.0,<7.0
