# سجل التغييرات

## v0.2.0 - جاهزية إنتاجية موسَّعة (الأمان، المراقبة، التدقيق)

### الأمان (جديد)

- إضافة مصادقة عبر مفتاح API (رأس `X-API-Key`) بمقارنة بزمن ثابت (`hmac.compare_digest`) لكل نقاط النهاية الحسّاسة، مع وضع تطوير محلي صريح عند عدم ضبط أي مفتاح.
- إضافة تحديد معدّل الطلبات (rate limiting) بنافذة زمنية ثابتة لكل عميل (مفتاح API أو IP)، قابل للتهيئة عبر متغيرات البيئة.
- إضافة دعم CORS صريح (بلا أصول مفتوحة افتراضيًا) — يُفعَّل فقط عبر `SHARIA_AI_CORS_ORIGINS`.
- إضافة حدود صارمة لحجم المُدخلات (نصوص العقود) لمنع هجمات استنزاف الموارد (DoS).
- تشديد تحقّق Pydantic على البيانات المالية المُدخَلة (`market_cap > 0`، قيم غير سالبة لكل الحقول المالية).
- إضافة `SECURITY.md` يوثّق نموذج الأمان الكامل وحدوده المعروفة صراحةً.

### المراقبة والتدقيق (جديد)

- إضافة تسجيل منظَّم بصيغة JSON (`utils/logging_setup.py`) مع معرِّف طلب فريد (`X-Request-ID`) لكل استدعاء.
- إضافة معالِج استثناءات عام يمنع تسرّب تفاصيل الأخطاء الداخلية إلى المستخدم النهائي.
- إضافة سجلّ تدقيق دائم (`audit/audit_log.py`) قائم على SQLite، يُخزِّن كل قرار امتثال صادر عن النظام؛ متاح عبر نقطة نهاية محمية `/v1/audit/recent`.

### البنية التحتية

- استعادة خط أنابيب CI (`.github/workflows/ci.yml`) وDependabot، بعد أن كانا مفقودين من شجرة العمل رغم وجودهما في تاريخ المستودع.
- إصدار API تحت البادئة `/v1/*` لتفادي كسر التوافق مستقبلاً.
- إضافة `.env.example` و`docker-compose.yml` (بوحدة تخزين دائمة لسجلّ التدقيق) و`Dockerfile` مُحدَّث.

### الاختبارات والجودة

- إضافة 63 اختبارًا جديدًا (من 93 إلى 156 إجمالًا) تغطي: المصادقة، تحديد معدّل الطلبات، سجلّ التدقيق، التسجيل المنظَّم، CORS، ودورة حياة التطبيق (lifespan).
- الحفاظ على تغطية 100% وRuff/mypy نظيفَين طوال هذا التوسيع.

## v0.1.0 - الإصدار الأول

### جاهزية الإنتاج

- إضافة خط CI كامل لتشغيل الاختبارات، التغطية، Ruff، وmypy على Python 3.10 و3.11 و3.12.
- إعادة تفعيل Dependabot لمتابعة تحديثات تبعيات Python وGitHub Actions.
- تثبيت تبعية `httpx2` اللازمة لاختبارات FastAPI/Starlette الحديثة.
- رفع تغطية الاختبارات إلى 100% مع بوابة فشل صارمة عند الانخفاض عن 100%.

### الاختبارات والجودة

- توسيع الاختبارات لتغطية ملخصات فحص الأسهم، كشف العقود، حاسبة الزكاة، خط الامتثال، والتحويل الآمن إلى JSON.
- تحسين اختبارات الكائنات المجمدة لاستخدام `FrozenInstanceError` بدلا من التقاط استثناء عام.
- التحقق من الفروع الدفاعية في واجهة API ومن تكامل المصنف الهجين الاختياري.

### إصلاحات

- تحسين تحويل قيم Enum النصية داخل تقارير الامتثال حتى تكون مخرجات JSON صريحة ومستقرة.
